// define global vars here

// OpenLayers.Map object
var map;

// Dynamic country feature store
var countryFS;

// Indicator store to populate the ComboBox
var indicatorStore;

// ComboBoxes
var indComboBox;
var yearComboBox;
var clTypeComboBox;
var clComboBox;

// Vector Legend
var vectorLegend;

// Array containing selected features
var selectedFeatures = new Array();
